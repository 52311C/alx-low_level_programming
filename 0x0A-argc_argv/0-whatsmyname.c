#include <stdio.h>
#include <stdio.h>

/**
 * main - code entry
 * Description: Program to print its name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("This program's name is %s\n", argv[0]);
	return (0);
}
